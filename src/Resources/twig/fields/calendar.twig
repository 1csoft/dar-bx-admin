{% extends 'form.row.twig' %}
{% block field %}
    {% if item.isDisabled() %}
        {{ value }}
    {% else %}
        <input size="47" type="text" name="{{ name }}" class="{{ styleClass }} calendar_once" value="{{ value }}" />
    {% endif %}
{% endblock %}